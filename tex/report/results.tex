\section{Results}
\label{sec:results}

The learning curves for the variational lower bound can be seen in Figure~\ref{fig:learning_curves} for different values of both downsampling factors and latent sizes,
and the final values for the variational lower bounds for the two downsampling factors compared to the variational lower bound without downsampling can be seen in Table~\ref{tab:variational_lower_bound}.

Samples from the training set can be seen in Figure~\ref{fig:samples} showing the originals, the preprocessed versions, as well as reconstrions using both bicubic interpolation and the VAE.
The reconstructions using the VAE show the mean of the Bernoulli distribution for each pixel.

\begin{figure*}
    \centering
	\hspace*{\fill}
    \subbottom[\label{fig:learning_curves:downsampling_factor}]{
        \includegraphics[width = .45\linewidth]{learning_curves_downsampling_bs_ds_l30_e50}
    }
    \hfill
    \subbottom[\label{fig:learning_curves:latent_size}]{
        \includegraphics[width = .45\linewidth]{learning_curves_latent_size_bs_ds2_l_e50}
    }
	\hspace*{\fill}
    \caption{Learning curves for 
        \subcaptionref{fig:learning_curves:downsampling_factor} both training and test sets for different downsampling factors $d$ with a latent size of $N_{\vec{z}} = 30$
        and
        \subcaptionref{fig:learning_curves:latent_size} only the training set for different latent sizes $N_{\vec{z}}$ using a downsampling factor of $d = 2$.
    }
    \label{fig:learning_curves}
\end{figure*}

\begin{table}
    \centering
    \caption{
        The differences in variational lower bound $\mathcal{L}\order{d}$ for the two downsampling factors after $50$ epochs for both training and test set compared to the variational lower bound without downsampling, $\mathcal{L}\order{d = 1}$.
    }
    \label{tab:variational_lower_bound}
    % \setlength{\tabcolsep}{1em}
    \sisetup{
        table-number-alignment = center
    }
    \begin{tabular}{
            l@{}
            S[table-format = 2.0]
            S[table-format = 1.0]
            S[table-format = -3.2]
            S[table-format = -3.2]
            S[table-format = -2.2]
            S[table-format = -2.2]
        }
        \toprule
        & {$N_{\vec{z}}$} & {$d$}
        & {$\mathcal{L}\idx{train}\order{d} - \mathcal{L}\idx{train}\order{d = 1}$}
        & {$\mathcal{L}\idx{test}\order{d} - \mathcal{L}\idx{test}\order{d = 1}$} \\
        \midrule
        \input{../../data/results_variational_lower_bound}
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}
    \centering
    \subbottom[$d = 2$.\label{fig:samples:downsampling:2}]{
        \includegraphics[width=.9\linewidth]{reconstructions_bs_ds2_l30_e50.pdf}
    }
    \\
    \subbottom[$d = 4$.\label{fig:samples:downsampling:4}]{
        \includegraphics[width=.9\linewidth]{reconstructions_bs_ds4_l30_e50.pdf}
    }
    \caption{
        Samples from the test set for the two downsampling factors.
        \emph{(from top to bottom in each subfigure)}
        The originals,
        the binarised versions using Bernoulli sampling,
        the downsampled versions using a mean filter, and lastly
        both the upscaled reconstructions using bicubic interpolation as well as the VAE with a latent size $N_{\vec{z}}$ of 30.
    }
    \label{fig:samples}
\end{figure}
