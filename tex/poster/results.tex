\section{Results}
\label{sec:results}

\begin{columnfigure}
	\centering
	\includegraphics[width=\linewidth]{learning_curves_b_ds_l30_e50}
	\figcaption{Learning curves for both training and test sets for differing downsampling factors $d$ with a latent size of $N_{\vec{z}} = 30$.}
\end{columnfigure}

\begin{columntable}
	\centering
	\tabcaption{The variational lower bound after $50$ epochs for both training and test set as well as the differences from the variational lower bound without downsampling, $\mathcal{L}\order{d = 1}$, for different values of latent size $N_{\vec{z}}$ and downsampling factor $d$.}
	\setlength{\tabcolsep}{1em}
	\sisetup{
		table-number-alignment = center
	}
	\begin{tabular}{
			l@{}
			S[table-format = 2.0]
			S[table-format = 1.0]
			S[table-format = -3.2]
			S[table-format = -3.2]
			S[table-format = -2.2]
			S[table-format = -2.2]
		}
		\toprule
		& {$N_{\vec{z}}$} & {$d$} & {$\mathcal{L}\idx{train}$}
		& {$\mathcal{L}\idx{train} - \mathcal{L}\idx{train}\order{d = 1}$}
		& {$\mathcal{L}\idx{test}$}
		& {$\mathcal{L}\idx{test} - \mathcal{L}\idx{test}\order{d = 1}$} \\
		\midrule
		\input{../../data/results_variational_lower_bound}
		\bottomrule
	\end{tabular}
\end{columntable}

\begin{columnfigure}
	\centering
	\includegraphics[width=\linewidth]{reconstructions_set_b_ds_l30_e50.pdf}
	\figcaption{Sampled numbers from the test set for two downsampling factors (given above the numbers), their downsampled versions as well as upscaled reconstruction of the downsampled versions using bicubic interpolation and variational auto-encoding.}
\end{columnfigure}
% TODO Include homemade numbers in samples?

% TODO: Learning rate
% TODO: Hidden size
% TODO: Bernoulli sampling
