\section{Results}
\label{sec:results}

\begin{columnfigure}
	\centering
	\includegraphics[width=\linewidth]{learning_curves_ds_l30_e50}
	\figcaption{Learning curves for both training and test sets for differing downsampling factors $d$ with a latent size of $N_{\vec{z}} = 30$.}
\end{columnfigure}

\begin{columntable}
	\centering
	\tabcaption{The variational lower bound after $50$ epochs for both training and test set as well as the difference from the variational lower bound without downsampling, $\mathcal{L}\idx{test}\order{d = 1}$, for the test set.}
	\setlength{\tabcolsep}{1em}
	\sisetup{
		table-number-alignment = center
	}
	\begin{tabular}{
			l@{}
			S[table-format = 2.0]
			S[table-format = 1.0]
			S[table-format = -3.2]
			S[table-format = -3.2]
			S[table-format = -2.2]
		}
		\toprule
		& {$N_{\vec{z}}$} & {$d$} & {$\mathcal{L}\idx{train}$} & {$\mathcal{L}\idx{test}$} & {$\mathcal{L}\idx{test} - \mathcal{L}\idx{test}\order{d = 1}$} \\
		\midrule
		\input{../../data/results_variational_lower_bound}
		\bottomrule
	\end{tabular}
\end{columntable}

\begin{columnfigure}
	\centering
	\includegraphics[width=\linewidth]{reconstructions_set_ds_l30_e50.pdf}
	\figcaption{Sampled numbers from the test set for two downsampling factors (given above the numbers), their downsampled versions as well as upscaled results using \rephrase{a bicubic method} and a varitional auto-encoder method.}
\end{columnfigure}

% TODO: Learning rate
% TODO: Hidden size
% TODO: Bernoulli sampling
